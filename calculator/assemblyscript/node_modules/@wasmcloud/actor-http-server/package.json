{
  "name": "@wasmcloud/actor-http-server",
  "version": "0.1.4",
  "description": "Interface to the httpserver contract for use by wasmCloud Actors",
  "author": "wasmCloud Team",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "asc assembly/index.ts -b build/http-server.wasm --use abort=assembly/index/abort --optimize"
  },
  "license": "ISC",
  "dependencies": {
    "@wapc/as-guest": "^v0.2.1",
    "@wapc/as-msgpack": "^0.1.11"
  },
  "devDependencies": {
    "assemblyscript": "^0.17.1"
  }
}